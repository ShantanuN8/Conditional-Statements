#include <iostream>

using namespace std;
int main()
{
    int myinput;
    cout<<"Enter a number:-";
    cin>>myinput;
    if(myinput==0){
        cout<<"Number is zero";
    }
    else if(myinput%2==0){
        cout<<"Number is even";
    }
    else{
        cout<<"Number is odd";
    }
}
